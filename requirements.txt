# DART 골프장 감사보고서 모니터링 봇 - 의존성 패키지

# 엑셀 파일 처리
pandas>=2.0.0
openpyxl>=3.1.0

# HTTP 요청
requests>=2.31.0

# 환경 변수 관리
python-dotenv>=1.0.0

# 로깅 (기본 포함되어 있지만 명시)
# logging은 Python 표준 라이브러리이므로 별도 설치 불필요
